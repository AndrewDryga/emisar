# .agent/loop.yaml — configuration for `coop loop` (committed; edit to taste).
# Every key is optional — omit it (or a whole section) to use coop's built-in default.
# Models use coop's target grammar: provider[:model][/effort][@account].
# Sections are in loop order: preflight (before) → work → between (after each task) → signoff (at the end).
#
# The frontier preset owns work routing. Routine between-task audits remain disabled;
# protected-change audits and final signoff retain their existing enforcement.

preflight:
  # The one-shot pre-loop queue tidy: coop itself returns blocked tasks whose decision.md
  # now has a Resolution to todo — host-side, no box, no model. Deliberately NO extra
  # LLM prompt here: an LLM self-containment audit in this slot over-blocked healthy
  # tasks when coop dogfooded it (it parked five self-contained tasks in one run).
  enabled: true

work:
  # The preset owns the lead's model fallback and thinker/critic/fast roles.
  agent: [frontier]

between:
  # Measured 2026-08-01 over 49 audits: 4.1 min on a ~12 min task (~31% of runtime) for 3 reopens
  # (6%). Turned off for throughput — this only drops the ROUTINE per-task audit. Coop still runs a
  # MANDATORY protected-change audit whenever a task touches gate-defining files (Makefile, loop.yaml,
  # hooks, workflows), and that one stops the loop on failure, so the gate can never be weakened
  # unreviewed. Signoff still reviews the whole run at the end.
  enabled: false

  # Review boxes write task queues only; the repository stays read-only.
  writes: tasks

  # Coop now anchors receipt parsing past the Codex wrapper footer, so Luna leads again.
  agent: [codex:gpt-5.6-luna/xhigh, claude:claude-opus-4-8/xhigh]

  # coop prepends the just-finished task's id + folder, so "the task named above" is exact.
  prompt: |
    Audit ONLY the task named above — nothing else. Hold it against its own task.md,
    trusting evidence over prose:
    - Every acceptance criterion is demonstrably met — point at the commit, diff, or test
      that proves it, not at a claim in log.md.
    - The failure/denial path is tested, not just the happy path — this is a security
      product; a new ingestion surface without an abuse-path test is a fail.
    - The gate of EVERY project the diff touched ran green on the committed tree — the
      exact command is in that project's AGENTS.md (portal/runner/mcp/packs/infra).
    - The diff serves the task's authorized outcome and accepted constraints.
      Necessary implementation choices need not be named in advance. Unrelated
      features or refactors are scope expansion — reopen with instructions to split.
    - No production-applied migration was edited or deleted. An existing migration
      may be corrected only with evidence that production has not run it; Git history
      alone is not deployment evidence. Follow portal/AGENTS.md section 8.
    - The commit says WHY and carries its `Coop-Task: <id>` trailer.
    A FAIL means a defect in the work itself. An acceptance check this box cannot run
    (root, docker, platform) is NOT a defect: first try to gather the evidence yourself
    the way the manual demands of every agent; when the box genuinely cannot produce it
    and the task's log states the limit plus compensating evidence (CI coverage, an
    alternate harness), verify THAT evidence and report the residual gap in your receipt
    — do not reopen. A reopen for a check the work box also could not run just forces a
    verification-only rework with no commit to bind, which the completion window rejects.
    Verdict: REPORT it — never apply it. Your verdict and evidence go in your OUTPUT
    (the receipt lines coop asks for), with a failed audit's concrete gap — what to fix,
    where — stated right above the verdict line. Do not move task folders, do not edit
    anything under .agent/tasks: coop applies the verdict host-side with proper leasing,
    and its completion audit rejects the whole cycle when the review stage touches the
    queue (a manual reopen also strands the task's lease, which gets the NEXT
    iteration's completion rejected as unleased). Never widen scope beyond the audited
    task.
    Receipt format is parsed strictly from the END of your reply: the last non-empty
    line must BE the single `REVIEW COMPLETE — …` line, with the `AUDIT EVIDENCE — …`
    line(s) immediately before it — nothing after them, no summary, no sign-off. Emit
    each of those lines exactly once in the whole reply (a restated receipt earlier, or
    any text after the final line, voids the verdict and kills the run).
    The `findings:` field is EITHER the literal word `none` OR the unresolved findings
    themselves; put residual notes and caveats in the prose above the evidence lines,
    never inside the `findings:` field.

signoff:
  # Signoff writes task queues only.
  writes: tasks

  # Reverse the lead's provider order for final review; retain the higher review effort.
  agent: [codex:gpt-6-astra/xhigh, claude:claude-fable-5-1/xhigh]

  # Extra checks APPENDED to coop's built-in senior review (goal, standards,
  # failure-path tests, polish, the gate). The reviewer reopens a task that fails one.
  prompt: |
    - The gate of every project the batch touched is green (exact command in that
      project's AGENTS.md); root-level agent-setup changes keep
      `./run check agent-setup` passing.
    - Production-applied migrations are immutable; edits to an existing migration
      carry evidence it is unrun. Follow portal/AGENTS.md section 8.
    - Every new surface that ingests runner / LLM / operator input has its abuse case
      tested (the denial path, not just the happy path).
    - Scope discipline: each diff serves its task's authorized outcome and accepted
      constraints. Necessary implementation choices need not be named in advance;
      reopen unrelated features or refactors to split them into separate work.
    - No new dependency without a one-line justification in the commit message.
    - Reusable corrections update their existing rule owner or an indexed,
      domain-prefixed rule. Task-specific directions do not become universal rules.
    - The commit contains only task-owned changes; unrelated WIP is preserved and
      no untracked task deliverable is left behind.
    - A user-visible change updated the website documentation, README, KB
      specification/card/runbook, or affected project guide it invalidated.

verify:
  # Verify writes task queues only.
  writes: tasks
