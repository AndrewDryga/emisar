<div class="bg-zinc-950 text-zinc-100">
  <.marketing_nav current_user={@current_user} current={:docs} />

  <section class="py-12 sm:py-16">
    <.docs_layout
      current="integrations-keycloak"
      updated="August 25, 2026"
      evidence="Live-tested against Keycloak on August 25, 2026: TLS OIDC sign-in and the emisar SCIM protocol suite passed. Keycloak does not provide stock outbound SCIM."
      toc={[
        {"sso", "Single sign-on"},
        {"scim", "Directory sync"}
      ]}
    >
      <.docs_header current="integrations-keycloak">
        <:dek>
          Connect Keycloak to emisar: a confidential OIDC client with PKCE for sign-in. Keycloak has
          no outbound SCIM, and the directory-sync section shows what that leaves you to do.
        </:dek>
      </.docs_header>
      <.docs_prerequisites>
        <:item><strong class="text-zinc-300">Keycloak realm-admin</strong> access.</:item>
        <:item>An emisar <strong class="text-zinc-300">owner or admin</strong>.</:item>
      </.docs_prerequisites>
      <p class="mt-5 text-base leading-7 text-zinc-400">
        The screenshots use Keycloak 26.7. Earlier versions arrange the same settings differently.
      </p>

      <.docs_h2 id="sso">Single sign-on</.docs_h2>
      <p class="mt-5 text-base leading-7 text-zinc-400">
        Connecting an identity provider is a paid feature. <.plan_note tier={:team} />
      </p>
      <ol class="mt-8 space-y-10">
        <li class="flex gap-4">
          <span class="grid h-8 w-8 flex-none place-items-center rounded-full bg-brand-500/20 text-sm font-semibold text-brand-300">
            1
          </span>
          <div class="min-w-0 text-base leading-7 text-zinc-400">
            <.docs_step_venue>emisar</.docs_step_venue>
            <p class="font-semibold text-zinc-100">Start the connection.</p>
            <p class="mt-1.5">
              Open
              <a
                href={~p"/app/sso/new"}
                class="font-medium text-brand-400 underline decoration-zinc-700 underline-offset-4 hover:text-brand-300"
              >Team → Single sign-on → Add provider</a>
              and choose <strong class="text-zinc-200">Keycloak</strong>. Leave the page open: the
              callback URL on it is what Keycloak asks for next, and Keycloak's values come back here
              at the end.
            </p>
            <.docs_screenshot
              src="/images/docs/sso/keycloak-emisar-add-provider.webp"
              title="emisar · Add provider"
              caption="Picking the provider here tailors the rest of the form to it."
              alt="emisar's Add provider form with the Provider type select outlined in green and set to Keycloak."
            />
          </div>
        </li>
        <li class="flex gap-4">
          <span class="grid h-8 w-8 flex-none place-items-center rounded-full bg-brand-500/20 text-sm font-semibold text-brand-300">
            2
          </span>
          <div class="min-w-0 text-base leading-7 text-zinc-400">
            <.docs_step_venue>Keycloak</.docs_step_venue>
            <p class="font-semibold text-zinc-100">Start an OpenID Connect client.</p>
            <p class="mt-1.5">
              In the Keycloak Admin Console, choose your realm, then go to <strong class="text-zinc-200">Clients → Create client</strong>. Leave
              <strong class="text-zinc-200">Client type</strong>
              on <span class="text-zinc-300">OpenID Connect</span>, set
              <strong class="text-zinc-200">Client ID</strong>
              to <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">emisar-portal</code>,
              and click <strong class="text-zinc-200">Next</strong>.
            </p>
            <.docs_screenshot
              src="/images/docs/sso/keycloak-client-general.webp"
              title="Keycloak Admin Console · General settings"
              caption="Create an OpenID Connect client for the emisar web application."
              alt="Keycloak's Create client General settings step with OpenID Connect selected, emisar-portal as the client ID, and emisar as the name."
            />
          </div>
        </li>
        <li class="flex gap-4">
          <span class="grid h-8 w-8 flex-none place-items-center rounded-full bg-brand-500/20 text-sm font-semibold text-brand-300">
            3
          </span>
          <div class="min-w-0 text-base leading-7 text-zinc-400">
            <.docs_step_venue>Keycloak</.docs_step_venue>
            <p class="font-semibold text-zinc-100">Enable only the flow emisar uses.</p>
            <p class="mt-1.5">
              Set the capability controls before continuing:
            </p>
            <.docs_settings>
              <:setting label="Client authentication">
                <strong class="text-zinc-200">On.</strong>
              </:setting>
              <:setting label="Standard flow">
                <strong class="text-zinc-200">On.</strong>
              </:setting>
              <:setting label="Require PKCE">
                <strong class="text-zinc-200">On</strong>, with method <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">S256</code>.
              </:setting>
              <:setting label="Every other flow">
                <strong class="text-zinc-200">Off.</strong> This includes Authorization, DPoP,
                Direct access grants, Implicit flow, Service accounts roles, and both device flows.
              </:setting>
            </.docs_settings>
            <p class="mt-4">
              Select <strong class="text-zinc-200">Next</strong>.
            </p>
            <.docs_screenshot
              src="/images/docs/sso/keycloak-client-capabilities.webp"
              title="Keycloak Admin Console · Capability config"
              caption="A confidential client using the authorization-code flow with PKCE."
              alt="Keycloak's Capability config step with Client authentication, Standard flow, and Require PKCE enabled; all other capabilities and authentication flows are disabled."
            />
          </div>
        </li>
        <li class="flex gap-4">
          <span class="grid h-8 w-8 flex-none place-items-center rounded-full bg-brand-500/20 text-sm font-semibold text-brand-300">
            4
          </span>
          <div class="min-w-0 text-base leading-7 text-zinc-400">
            <.docs_step_venue>Keycloak</.docs_step_venue>
            <p class="font-semibold text-zinc-100">Register the exact redirect URI.</p>
            <p class="mt-1.5">
              Copy the callback shown in emisar and paste it into <strong class="text-zinc-200">Valid redirect URIs</strong>. It ends in <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">/sign_in/sso/callback</code>.
              Do not add a wildcard. The other URL fields can stay empty. Select <strong class="text-zinc-200">Save</strong>.
            </p>
            <.docs_screenshot
              src="/images/docs/sso/keycloak-client-redirect.webp"
              title="Keycloak Admin Console · Login settings"
              caption="The screenshot uses a reserved example host; paste the callback from your emisar form verbatim."
              alt="Keycloak's Login settings step with https://emisar.dev/sign_in/sso/callback entered as the sole Valid redirect URI and no wildcard."
            />
          </div>
        </li>
        <li class="flex gap-4">
          <span class="grid h-8 w-8 flex-none place-items-center rounded-full bg-brand-500/20 text-sm font-semibold text-brand-300">
            5
          </span>
          <div class="min-w-0 text-base leading-7 text-zinc-400">
            <.docs_step_venue>Keycloak</.docs_step_venue>
            <p class="font-semibold text-zinc-100">Copy the client secret.</p>
            <p class="mt-1.5">
              Open the saved client's <strong class="text-zinc-200">Credentials</strong>
              tab. Copy its client secret into emisar's
              <strong class="text-zinc-200">Client secret</strong>
              field. Treat the client secret like a password. Do not put it in tickets, chat, or
              screenshots.
            </p>
            <.docs_screenshot
              src="/images/docs/sso/keycloak-client-secret.webp"
              title="Keycloak Admin Console · Credentials"
              caption="The client secret stays masked in this guide."
              alt="The Keycloak Credentials tab for emisar-portal with Client Id and Secret selected and the client secret hidden behind password dots."
            />
          </div>
        </li>
        <li class="flex gap-4">
          <span class="grid h-8 w-8 flex-none place-items-center rounded-full bg-brand-500/20 text-sm font-semibold text-brand-300">
            6
          </span>
          <div class="min-w-0 text-base leading-7 text-zinc-400">
            <.docs_step_venue>Keycloak</.docs_step_venue>
            <p class="font-semibold text-zinc-100">Verify the identity scopes.</p>
            <p class="mt-1.5">
              On the <strong class="text-zinc-200">Client scopes</strong>
              tab, confirm that <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">email</code>
              and <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">profile</code>
              are assigned as <span class="text-zinc-300">Default</span>. The authorization request includes <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">openid</code>, so Keycloak does
              not list it as an assigned client scope.
            </p>
            <.docs_screenshot
              src="/images/docs/sso/keycloak-client-scopes.webp"
              title="Keycloak Admin Console · Client scopes"
              caption="The default email and profile scopes give emisar the verified identity claims it needs."
              alt="The Keycloak Client scopes table for emisar-portal with email and profile assigned as Default scopes."
            />
          </div>
        </li>
        <li class="flex gap-4">
          <span class="grid h-8 w-8 flex-none place-items-center rounded-full bg-brand-500/20 text-sm font-semibold text-brand-300">
            7
          </span>
          <div class="min-w-0 text-base leading-7 text-zinc-400">
            <.docs_step_venue>emisar</.docs_step_venue>
            <p class="font-semibold text-zinc-100">Connect and test the first sign-in.</p>
            <p class="mt-1.5">
              Back on the connection you opened in step 1, paste the client ID and secret, and
              build the issuer from your Keycloak host and realm:
            </p>
            <.docs_value
              label="Issuer URL"
              copy_text="https://YOUR-KEYCLOAK-HOST/realms/YOUR-REALM"
            >
              https://YOUR-KEYCLOAK-HOST/realms/YOUR-REALM
            </.docs_value>
            <p class="mt-4">
              Press <strong class="text-zinc-200">Test connection</strong>
              — it fetches the realm's discovery document, so a wrong host or realm fails here
              rather than at someone's first sign-in. Then tick
              <strong class="text-zinc-200">Enabled</strong>
              and save. Open a private window and sign in as a Keycloak user who is not in emisar.
              Auto-provisioning assigns the default role. Manual provisioning creates a pending
              request for an administrator.
            </p>
            <.docs_callout kind={:warn} title="Your Keycloak has to be reachable from emisar">
              <p>
                emisar fetches the realm's discovery document, JWKS, and token endpoint from
                its own network, so the realm URL must resolve to a public address with a
                certificate from a public CA — private and internal addresses are refused.
              </p>
              <p class="mt-3">
                Publish an internal Keycloak through a gateway emisar can reach.
              </p>
            </.docs_callout>

            <.docs_screenshot
              src="/images/docs/sso/keycloak-emisar-credentials.webp"
              title="emisar · Add provider"
              caption="Three values from Keycloak. The issuer is the realm URL, not the host on its own."
              alt="emisar's OIDC connection fields with Issuer URL, Client ID and Client secret each outlined in green, and the Issuer URL placeholder showing https://YOUR-HOST/realms/YOUR-REALM."
            />
          </div>
        </li>
      </ol>

      <.docs_callout kind={:note} title="The rest of the connection form">
        <p>
          This guide covers only the provider-specific fields.
          <.link
            navigate={~p"/docs/sso"}
            class="text-brand-400 hover:text-brand-300"
          >Single sign-on</.link>
          explains provisioning, default roles, runner access, and provider
          MFA settings.
        </p>
      </.docs_callout>

      <.docs_h2 id="scim">Directory sync</.docs_h2>
      <p class="mt-5 text-base leading-7 text-zinc-400">
        Keycloak cannot push users to emisar — it has no outbound SCIM (its own SCIM Realm API
        only receives users into Keycloak). Sign-in still works: members are created on their
        first sign-in, automatically or held for approval, but roles are not mapped and nobody
        is removed automatically.
      </p>
      <p class="mt-5 text-base leading-7 text-zinc-400">
        Offboarding is therefore two steps: disable the user in Keycloak to block new
        sign-ins, then suspend or remove the member in
        <a
          href={~p"/app/team"}
          class="font-medium text-brand-300 underline decoration-brand-500/30 underline-offset-4 hover:text-brand-200"
        >Team</a>
        to end their sessions and revoke their API keys.
      </p>
      <p class="mt-5 text-base leading-7 text-zinc-400">
        A third-party Keycloak provisioning plugin can drive emisar's SCIM endpoint with the
        base URL and bearer token. emisar does not ship or certify one — you choose,
        configure, and support it. <.plan_note tier={:enterprise} />
      </p>
    </.docs_layout>
  </section>

  <.marketing_footer />
</div>
