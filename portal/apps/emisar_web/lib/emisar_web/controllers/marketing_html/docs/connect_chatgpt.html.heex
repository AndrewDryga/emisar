<div class="bg-zinc-950 text-zinc-100">
  <.marketing_nav current_user={@current_user} current={:docs} />

  <section class="py-12 sm:py-16">
    <.docs_layout
      current="connect-chatgpt"
      updated="August 21, 2026"
      toc={[
        {"connect", "Add the connector"},
        {"verify", "Verify the connection"},
        {"sign-in-grants", "What the sign-in grants"}
      ]}
    >
      <.docs_header current="connect-chatgpt">
        <:dek>
          Connect ChatGPT to emisar through Developer mode over remote MCP. OAuth signs you in —
          you do not need to copy an agent key.
        </:dek>
      </.docs_header>

      <p class="mt-6 text-base leading-7 text-zinc-400">
        Using Claude Code, Cursor, Claude Desktop, or a CLI instead? Those connect through the
        <.docs_inline_code>emisar-mcp</.docs_inline_code>
        stdio bridge — see <.link
          navigate={~p"/docs/connect-cli-agent"}
          class="text-brand-400 hover:text-brand-300"
        >Connect a CLI agent</.link>. For Claude.ai, see <.link
          navigate={~p"/docs/connect-claude-ai"}
          class="text-brand-400 hover:text-brand-300"
        >Claude.ai</.link>.
      </p>

      <.docs_prerequisites>
        <:item>
          An account with at least one runner online. Otherwise the agent signs in fine and finds
          an empty catalog with nothing to call. New here? Run the
          <.link navigate={~p"/docs/quickstart"} class="text-brand-400 hover:text-brand-300">
            quickstart
          </.link>
          first, then come back to wire in your LLM.
        </:item>
        <:item>
          Runners that do not require signed dispatch. Claude.ai and ChatGPT do not create emisar
          dispatch signatures, so a runner that requires one needs the
          <.link
            navigate={~p"/docs/connect-cli-agent"}
            class="text-brand-400 hover:text-brand-300"
          >local MCP bridge</.link>
          with a signing key and certificate instead.
        </:item>
      </.docs_prerequisites>

      <.docs_h2 id="connect">Add the connector</.docs_h2>
      <p class="mt-4 text-base text-zinc-400">
        ChatGPT reaches a custom MCP server through <strong>Developer mode</strong>. Access depends
        on your account and workspace policy.
      </p>
      <ol class="mt-4 list-decimal space-y-3 pl-6 text-base text-zinc-300">
        <li>
          Open <strong>Settings → Security and login</strong>. Turn on <strong>Developer mode</strong>.
        </li>
        <li>
          Open
          <a
            href="https://chatgpt.com/plugins"
            class="text-brand-400 hover:text-brand-300"
          >ChatGPT Plugins</a>
          and select the small <strong>+</strong>
          button just right of the search box — it is easy to miss.
        </li>
        <li>
          Fill the form:
          <ul class="mt-1 list-disc space-y-1 pl-6 text-zinc-400">
            <li><strong>Name</strong> — paste the name below</li>
            <li>
              <strong>Description</strong> (optional) — "Run approved
              infrastructure actions on my fleet."
            </li>
            <li>
              <strong>Connection</strong> → <strong>Server URL</strong> — paste the URL below
            </li>
            <li>
              <strong>Authentication</strong> — choose <strong>OAuth</strong>. There is no client
              ID or secret to paste; emisar registers the connector itself.
            </li>
          </ul>
          <.docs_value label="Name" copy_text="emisar">emisar</.docs_value>
          <.docs_value label="Remote MCP server URL" copy_text="https://emisar.dev/api/mcp/rpc">
            https://emisar.dev/api/mcp/rpc
          </.docs_value>
          <p class="mt-3">
            Select <strong>Create</strong>, then review the discovered tools and metadata.
          </p>
        </li>
        <li>
          ChatGPT opens the emisar sign-in page. Sign in and choose <strong>Authorize</strong>. ChatGPT then lists the emisar tools.
        </li>
        <li>
          Review the connection permissions. emisar policy, approvals, and audit still apply to
          every call.
        </li>
        <li>
          Start a new chat. Add <strong>emisar</strong>
          from the tools menu. A listed tool means metadata access only. Adding the connection
          lets the chat call those tools.
        </li>
        <li>
          After a tool metadata change, open the plugin connection. Refresh its tool metadata
          before the next test.
        </li>
      </ol>
      <.docs_h2 id="verify">Verify the connection</.docs_h2>
      <ol class="mt-4 list-decimal space-y-3 pl-6 text-base text-zinc-300">
        <li>
          Ask the client to run <code>linux.uptime</code> on any one of the connected runners.
        </li>
        <li>Confirm that the client returns the action output.</li>
        <li>
          Open <a
            href={~p"/app/audit"}
            class="font-medium text-brand-300 underline decoration-brand-500/30 underline-offset-4 hover:text-brand-200"
          >Audit</a>. Confirm that the event records the client, runner, action, and reason.
        </li>
      </ol>

      <.docs_h2 id="sign-in-grants">What the sign-in grants</.docs_h2>
      <p class="mt-4 text-base leading-7 text-zinc-400">
        The consent screen mints a key bound to <em>your</em>
        membership. The connector acts as you. Every call is attributed to you and the agent you are using. It reaches
        only the runners and actions your own access already allows (<.link
          navigate={~p"/docs/agents-and-keys#no-scope"}
          class="text-brand-400 hover:text-brand-300"
        >how a key inherits its operator's scope</.link>). Revoke it any time from
        <strong>AI agents</strong>
        in the console, or from the connector settings in Claude or ChatGPT.
      </p>
    </.docs_layout>
  </section>

  <.marketing_cta
    headline="Point your agent at real infrastructure — safely."
    subcopy="Set up a runner first, then connect ChatGPT to your account's MCP endpoint and let policy decide what runs."
    secondary_label="Read the quickstart"
    secondary_path={~p"/docs/quickstart"}
  />

  <.marketing_footer />
</div>
