<div class="bg-zinc-950 text-zinc-100">
  <.marketing_nav current={:docs} />

  <section class="py-16 sm:py-24">
    <div class="mx-auto max-w-3xl px-6 lg:px-8">
      <p class="text-sm font-semibold text-indigo-400">Docs / Integration</p>
      <h1 class="mt-2 text-4xl font-bold tracking-tight text-zinc-50 md:text-5xl">
        Connect an LLM
      </h1>
      <p class="mt-6 text-lg leading-8 text-zinc-400">
        emisar exposes your runners and their action catalog as a Model
        Context Protocol server. Two ways to plug an LLM in:
      </p>

      <ul class="mt-4 list-disc space-y-1 pl-6 text-sm text-zinc-300">
        <li>
          <strong>Remote MCP</strong> (cloud LLMs, recent IDE clients) —
          paste a URL + API key. Works with Claude's <em>Custom Connectors</em>, ChatGPT's <em>Connectors</em>, and any
          client that speaks the MCP HTTP transport.
        </li>
        <li>
          <strong>Stdio bridge</strong> (Claude Desktop, Cursor, Claude
          Code, Codex CLI, Gemini CLI) — a tiny binary that runs on your
          laptop and proxies stdio MCP to the same HTTP endpoint.
        </li>
      </ul>

      <%!-- ============================================================ --%>
      <%!-- Step 1: API key                                              --%>
      <%!-- ============================================================ --%>
      <section class="mt-12">
        <div class="flex items-center gap-3">
          <span class="grid h-8 w-8 place-items-center rounded-full bg-indigo-500/20 text-sm font-semibold text-indigo-300">
            1
          </span>
          <h2 class="text-2xl font-semibold tracking-tight text-zinc-50">Mint an API key</h2>
        </div>

        <p class="mt-4 text-base leading-relaxed text-zinc-400">
          In the dashboard sidebar, click <code class="rounded bg-zinc-900 px-1.5 py-0.5 text-sm">Agents</code>.
          Pick your LLM client from the tab strip — we mint a key named
          after it and drop a prefilled snippet you can paste into the
          client's config. Quick keys are scoped to
          <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">actions:read</code>
          + <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">actions:execute</code>
          (the standard MCP shape).
        </p>

        <p class="mt-4 text-base leading-relaxed text-zinc-400">
          Want a tighter scope? Pick the
          <code class="rounded bg-zinc-900 px-1.5 py-0.5 text-sm">Custom</code>
          tab and restrict which runners or groups the key may target.
          The dashboard reveals the raw key once — copy and store it.
        </p>
      </section>

      <%!-- ============================================================ --%>
      <%!-- Step 2: Remote MCP                                            --%>
      <%!-- ============================================================ --%>
      <section class="mt-12">
        <div class="flex items-center gap-3">
          <span class="grid h-8 w-8 place-items-center rounded-full bg-indigo-500/20 text-sm font-semibold text-indigo-300">
            2
          </span>
          <h2 class="text-2xl font-semibold tracking-tight text-zinc-50">
            Option A — Remote MCP (recommended)
          </h2>
        </div>

        <p class="mt-4 text-base leading-relaxed text-zinc-400">
          Most modern MCP clients (Claude.ai web, ChatGPT web/Mac, recent
          Cursor, Continue, Zed) accept a remote MCP server. Two values
          to enter:
        </p>

        <div class="mt-4 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
          <div class="border-b border-zinc-900 px-3 py-2 font-mono text-[11px] text-zinc-500">
            connector settings
          </div>
          <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300">URL:        <span class="text-emerald-300">https://app.emisar.dev/api/mcp/rpc</span>
Auth header: <span class="text-emerald-300">Authorization: Bearer emk-...</span></pre>
        </div>

        <div class="mt-8 space-y-8">
          <%!-- Claude.ai (cloud) --%>
          <section>
            <h3 class="text-xl font-semibold text-zinc-50">Claude (claude.ai)</h3>
            <ol class="mt-2 list-decimal space-y-1 pl-6 text-sm text-zinc-300">
              <li>
                Open <strong>Settings → Connectors → Add custom connector</strong>.
              </li>
              <li>
                Set <strong>Remote MCP server URL</strong>
                to <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">https://app.emisar.dev/api/mcp/rpc</code>.
              </li>
              <li>
                Add an <strong>Authorization</strong>
                header: <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">Bearer emk-...</code>.
              </li>
              <li>
                Enable the connector in any chat that should reach your
                fleet. Claude lists the action catalog under the tool
                icon.
              </li>
            </ol>
          </section>

          <%!-- ChatGPT --%>
          <section>
            <h3 class="text-xl font-semibold text-zinc-50">ChatGPT (chat.openai.com)</h3>
            <ol class="mt-2 list-decimal space-y-1 pl-6 text-sm text-zinc-300">
              <li>
                Open <strong>Settings → Connectors</strong> (Pro / Team /
                Enterprise).
              </li>
              <li>
                Click <strong>Add connector</strong>, choose <strong>Custom MCP</strong>.
              </li>
              <li>
                Set the URL to
                <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">
                  https://app.emisar.dev/api/mcp/rpc
                </code>
                and the bearer token to your <code>emk-...</code>
                key.
              </li>
              <li>
                Toggle the connector on in any conversation; tools appear
                under the wrench icon.
              </li>
            </ol>
          </section>

          <%!-- Generic remote MCP --%>
          <section>
            <h3 class="text-xl font-semibold text-zinc-50">Any other MCP-HTTP client</h3>
            <p class="mt-2 text-sm text-zinc-400">
              Cursor, Continue, Zed, and bespoke agents that speak the
              MCP HTTP transport take the same URL + bearer. The endpoint
              implements JSON-RPC 2.0 with the standard <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">initialize</code>, <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">tools/list</code>, <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">tools/call</code>,
              and <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">ping</code> methods.
            </p>
            <div class="mt-3 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
              <div class="border-b border-zinc-900 px-3 py-2 font-mono text-[11px] text-zinc-500">
                terminal
              </div>
              <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300"><span class="text-zinc-500"># Try it from a shell — should return the tool list.</span>
<span class="text-zinc-500">$</span> curl -X POST <span class="text-emerald-300">https://app.emisar.dev/api/mcp/rpc</span> \
    -H <span class="text-emerald-300">"Authorization: Bearer $EMISAR_API_KEY"</span> \
    -H <span class="text-emerald-300">"Content-Type: application/json"</span> \
    -d <span class="text-emerald-300">'&#123;"jsonrpc":"2.0","id":1,"method":"tools/list"&#125;'</span></pre>
            </div>
          </section>
        </div>
      </section>

      <%!-- ============================================================ --%>
      <%!-- Step 3: Stdio bridge                                          --%>
      <%!-- ============================================================ --%>
      <section class="mt-12">
        <div class="flex items-center gap-3">
          <span class="grid h-8 w-8 place-items-center rounded-full bg-indigo-500/20 text-sm font-semibold text-indigo-300">
            3
          </span>
          <h2 class="text-2xl font-semibold tracking-tight text-zinc-50">
            Option B — Stdio bridge (desktop clients)
          </h2>
        </div>

        <p class="mt-4 text-base leading-relaxed text-zinc-400">
          For clients that only speak stdio MCP — Claude Desktop, Claude
          Code, Cursor (local mode), Gemini CLI, Codex CLI — install the
          tiny <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">emisar-mcp</code>
          binary. It's a thin shim that forwards stdio JSON-RPC frames
          to the same <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">/api/mcp/rpc</code>
          endpoint.
          Every MCP semantic — tool descriptors, content blocks,
          <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">wait_for_run</code>
          — is implemented in the portal,
          so the bridge stays version-locked to the spec without you
          redownloading it.
        </p>

        <h3 class="mt-6 text-xl font-semibold text-zinc-50">Install</h3>
        <p class="mt-2 text-sm text-zinc-300">
          One-liner — auto-resolves the latest
          <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">mcp-v*</code>
          release, verifies sha256, drops
          <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">emisar-mcp</code>
          into <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">/usr/local/bin</code>:
        </p>
        <div class="mt-2 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
          <div class="border-b border-zinc-900 px-3 py-2 font-mono text-[11px] text-zinc-500">
            terminal
          </div>
          <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300"><span class="text-zinc-500">$</span> curl -sSL <span class="text-emerald-300">https://emisar.dev/install-mcp.sh</span> | sudo bash</pre>
        </div>

        <p class="mt-4 text-sm text-zinc-300">
          <strong>
            Or <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">go install</code>
          </strong>
          (Go 1.26+):
        </p>
        <div class="mt-2 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
          <div class="border-b border-zinc-900 px-3 py-2 font-mono text-[11px] text-zinc-500">
            terminal
          </div>
          <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300"><span class="text-zinc-500"># `go install` builds the package directory's binary; the binary will</span>
<span class="text-zinc-500"># be ~/go/bin/mcp, so rename it on the symlink.</span>
<span class="text-zinc-500">$</span> go install github.com/andrewdryga/emisar/mcp@latest
<span class="text-zinc-500">$</span> sudo ln -sf ~/go/bin/mcp /usr/local/bin/emisar-mcp</pre>
        </div>

        <div class="mt-8 space-y-8">
          <%!-- Claude Desktop --%>
          <section>
            <h3 class="text-xl font-semibold text-zinc-50">Claude Desktop</h3>
            <p class="mt-2 text-sm text-zinc-400">
              Add to <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">~/Library/Application Support/Claude/claude_desktop_config.json</code>:
            </p>
            <div class="mt-3 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
              <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300">&#123;
  "mcpServers": &#123;
    "emisar": &#123;
      "command": "/usr/local/bin/emisar-mcp",
      "env": &#123;
        "EMISAR_URL": "https://app.emisar.dev",
        "EMISAR_API_KEY": "emk-..."
      &#125;
    &#125;
  &#125;
&#125;</pre>
            </div>
            <p class="mt-3 text-sm text-zinc-400">
              Restart Claude Desktop. Tools appear under the tool icon
              as "emisar".
            </p>
          </section>

          <%!-- Cursor --%>
          <section>
            <h3 class="text-xl font-semibold text-zinc-50">Cursor</h3>
            <p class="mt-2 text-sm text-zinc-400">
              Same bridge. Cursor reads <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">~/.cursor/mcp.json</code>:
            </p>
            <div class="mt-3 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
              <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300">&#123;
  "mcpServers": &#123;
    "emisar": &#123;
      "command": "emisar-mcp",
      "env": &#123;
        "EMISAR_URL": "https://app.emisar.dev",
        "EMISAR_API_KEY": "emk-..."
      &#125;
    &#125;
  &#125;
&#125;</pre>
            </div>
            <p class="mt-3 text-sm text-zinc-400">
              Recent Cursor builds also speak remote MCP — point them at
              <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">/api/mcp/rpc</code>
              directly if you'd rather skip
              the bridge.
            </p>
          </section>

          <%!-- Claude Code (CLI) --%>
          <section>
            <h3 class="text-xl font-semibold text-zinc-50">Claude Code</h3>
            <p class="mt-2 text-sm text-zinc-400">
              One-line register, user scope (every project):
            </p>
            <div class="mt-3 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
              <div class="border-b border-zinc-900 px-3 py-2 font-mono text-[11px] text-zinc-500">
                terminal
              </div>
              <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300"><span class="text-zinc-500">$</span> claude mcp add emisar /usr/local/bin/emisar-mcp \
    --scope user \
    -e EMISAR_URL=https://app.emisar.dev \
    -e EMISAR_API_KEY=emk-...</pre>
            </div>
            <p class="mt-3 text-sm text-zinc-400">
              Verify with <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">claude mcp list</code>.
            </p>
          </section>

          <%!-- Gemini CLI --%>
          <section>
            <h3 class="text-xl font-semibold text-zinc-50">Gemini CLI</h3>
            <p class="mt-2 text-sm text-zinc-400">
              Add to <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">~/.gemini/settings.json</code>:
            </p>
            <div class="mt-3 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
              <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300">&#123;
  "mcpServers": &#123;
    "emisar": &#123;
      "command": "/usr/local/bin/emisar-mcp",
      "env": &#123;
        "EMISAR_URL": "https://app.emisar.dev",
        "EMISAR_API_KEY": "emk-..."
      &#125;
    &#125;
  &#125;
&#125;</pre>
            </div>
          </section>

          <%!-- ChatGPT Codex CLI --%>
          <section>
            <h3 class="text-xl font-semibold text-zinc-50">ChatGPT Codex CLI</h3>
            <p class="mt-2 text-sm text-zinc-400">
              Add to <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">~/.codex/config.toml</code>:
            </p>
            <div class="mt-3 overflow-hidden rounded-lg border border-zinc-900 bg-black/60">
              <pre class="overflow-x-auto p-4 font-mono text-xs leading-6 text-zinc-300">[mcp_servers.emisar]
command = "/usr/local/bin/emisar-mcp"
env = &#123; EMISAR_URL = "https://app.emisar.dev", EMISAR_API_KEY = "emk-..." &#125;</pre>
            </div>
          </section>
        </div>
      </section>

      <%!-- ============================================================ --%>
      <%!-- REST endpoints                                                --%>
      <%!-- ============================================================ --%>
      <section class="mt-12">
        <div class="flex items-center gap-3">
          <span class="grid h-8 w-8 place-items-center rounded-full bg-indigo-500/20 text-sm font-semibold text-indigo-300">
            4
          </span>
          <h2 class="text-2xl font-semibold tracking-tight text-zinc-50">
            Option C — Plain HTTP / your own agent
          </h2>
        </div>

        <p class="mt-4 text-base leading-relaxed text-zinc-400">
          Same auth, same data, REST-shaped. Use these when wiring a
          custom agent (OpenAI tool calling, internal automation,
          generic curl).
        </p>

        <ul class="mt-4 space-y-2 text-sm text-zinc-300">
          <li>
            <code class="rounded bg-zinc-900 px-1.5 py-0.5 text-xs">GET /api/mcp/runners</code>
            — runners + the actions each advertises.
          </li>
          <li>
            <code class="rounded bg-zinc-900 px-1.5 py-0.5 text-xs">GET /api/mcp/tools</code>
            — one entry per distinct action with full JSON Schema
            input descriptor.
          </li>
          <li>
            <code class="rounded bg-zinc-900 px-1.5 py-0.5 text-xs">
              POST /api/mcp/tools/:action_id?wait=15s
            </code>
            — fire-and-forget or long-poll dispatch.
          </li>
          <li>
            <code class="rounded bg-zinc-900 px-1.5 py-0.5 text-xs">
              GET /api/mcp/runs/:id?wait=60s
            </code>
            — fetch result; long-poll for pending approvals.
          </li>
        </ul>

        <p class="mt-4 text-sm text-zinc-400">
          For OpenAI tool calling: pull <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">/api/mcp/tools</code>,
          forward each entry's
          <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">inputSchema</code>
          as a function parameter schema, and POST the model's chosen
          arguments back to <code class="rounded bg-zinc-900 px-1 py-0.5 text-xs">/api/mcp/tools/:name?wait=30s</code>.
          The <.link href={~p"/docs"} class="text-indigo-400 hover:text-indigo-300">docs</.link>
          have a full sample.
        </p>
      </section>
    </div>
  </section>

  <.marketing_footer />
</div>
