defmodule Emisar.Fixtures do
  @moduledoc """
  Namespace for test fixtures. Build rows through the per-domain submodules —
  `alias Emisar.Fixtures`, then `Fixtures.Accounts.create_account/1`,
  `Fixtures.Users.create_user/1`, `Fixtures.Memberships.create_membership/1`,
  `Fixtures.Subjects.subject_for/3`, … (see `Emisar.Fixtures.*`).
  """
end
