#!/bin/sh
# Set node name so Phoenix LiveView clustering can work. Override
# RELEASE_NODE / RELEASE_COOKIE for multi-node deploys.
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE="<%= @release.name %>@${POD_IP:-127.0.0.1}"
