#!/bin/sh
set -eu

# Run migrations before booting the endpoint. Failure here aborts the
# pod so the previous version keeps serving — exactly what we want.
"$(dirname "$0")/migrate"

# Start the release in the foreground; this is what Kubernetes /
# systemd / fly.io invokes.
exec "$(dirname "$0")/emisar" start
