# The ban lands in the netfilter namespace of whatever container fail2ban
# runs in, not the host's, once the lane grants NET_ADMIN. Declared because
# that lane is not built.
# See .agent/kb/pack-behavior-environment-limits.md for what was measured.
risk_accountability:
  mode: complete
  exceptions:
    f2b.banip: requires_privileged_host
