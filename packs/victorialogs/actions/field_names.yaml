schema_version: 1
id: vl.field_names
title: GET /select/logsql/field_names
kind: script
risk: low
description: >
  List the log field names matching a query, each with a hit count. Use to
  discover what fields exist before querying. Doubles as the pack's
  connectivity check.
side_effects:
  - One read-only HTTP GET to the VictoriaLogs query API.
  - Read-only.
args:
  - name: window
    type: string
    required: false
    default: "1h"
    description: 'Trailing search window ending now, up to 24h.'
    validation:
      pattern: '^[1-9][0-9]{0,4}[smhdw]$'
  - name: query
    type: string
    required: false
    default: "*"
    description: 'LogsQL scope, e.g. app:api. Defaults to all logs in the bounded window.'
    validation:
      pattern: '^[\s\S]{1,1000}$'
execution:
  script:
    path: scripts/vlget.sh
    interpreter: /bin/sh
  argv:
    - "{{ args.window }}"
    - "-"
    - "/select/logsql/field_names"
    - "--data-urlencode"
    - "query={{ args.query }}"
  timeout: 60s
output:
  parser: json
  max_stdout_bytes: 1048576
  max_stderr_bytes: 8192
examples:
  - title: All field names
    args: {}
