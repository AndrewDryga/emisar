#!/bin/sh
set -eu

[ "$#" -eq 4 ]
[ "$1" = "-k" ]
[ "$2" = "--no-pager" ]
[ "$3" = "-n" ]

printf '%s\n' \
  'packtest-kernel-oldest' \
  'packtest-kernel-benign' \
  'kernel: Out of memory: Killed process 4242 (fixture)' \
  'kernel: Killed process 4242 (fixture) total-vm:1000kB'
