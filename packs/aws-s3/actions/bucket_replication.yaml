schema_version: 1
id: s3.bucket_replication
title: aws s3api get-bucket-replication
kind: exec
risk: low
description: Show one bucket's replication role and rules.
side_effects: ["One API call.", "Read-only."]
args:
  - name: bucket
    type: string
    required: true
    description: Bucket name.
    validation: {pattern: "^[a-z0-9.\\-]{3,63}$"}
execution:
  command:
    binary: aws
    argv: [s3api, get-bucket-replication, --bucket, "{{ args.bucket }}", --output, json]
  timeout: 30s
output:
  parser: json
  parser_required: true
  max_stdout_bytes: 65536
  max_stderr_bytes: 8192
examples:
  - title: Replication
    args: {bucket: my-bucket}
