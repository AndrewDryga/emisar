schema_version: 1
id: gcp.http_proxies
title: gcloud compute target-http-proxies list
kind: script
risk: low
description: List bounded HTTP target proxies and their URL maps.
side_effects: ["Read-only Compute Engine API pagination."]
args:
  - name: project
    type: string
    required: true
    description: Google Cloud project ID.
    validation: {pattern: "^[a-z][a-z0-9-]{4,28}[a-z0-9]$", max_length: 30}
  - name: limit
    type: integer
    required: false
    default: 100
    description: Maximum HTTP proxies to return.
    validation: {min: 1, max: 500}
execution:
  script: {path: scripts/load_balancing.sh, interpreter: /bin/sh}
  argv: [http-proxies, "{{ args.project }}", "{{ args.limit }}"]
  timeout: 60s
output:
  parser: json
  parser_required: true
  max_stdout_bytes: 1048576
  max_stderr_bytes: 16384
examples:
  - title: Project HTTP proxies
    args: {project: example-prod}
