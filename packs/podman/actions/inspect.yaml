schema_version: 1
id: podman.inspect
title: podman inspect <id>
kind: exec
risk: low
description: >
  Show full container details (state, mounts, network config, env, labels)
  for one ID/name. Read-only.
side_effects: ["One CLI call.", "Read-only."]
args:
  - name: container
    type: string
    required: true
    description: Container ID or name.
    validation:
      pattern: "^[a-zA-Z0-9_.][a-zA-Z0-9_.\\-]{0,63}$"
execution:
  command: {binary: podman, argv: ["inspect", "{{ args.container }}"]}
  timeout: 30s
output:
  parser: json
  max_stdout_bytes: 524288
  max_stderr_bytes: 8192
examples:
  - title: One container
    args:
      container: "web"
