schema_version: 1
id: supertokens.tenants
title: List tenants and their login methods
kind: script
risk: low
description: >
  List every tenant with GET /recipe/multitenancy/tenant/list/v2 (core 9.1 or
  later): its id, first and required secondary factors (null means the core
  default), third-party provider ids, and the names of its core config
  overrides. Client secrets and config values are dropped on the host.
side_effects:
  - One read-only HTTP GET to the core's tenant list endpoint.
  - Read-only — never writes or mutates data.
search_terms:
  - login methods
  - social login broken
  - tenant config
execution:
  script:
    path: scripts/supertokens_api.sh
    interpreter: /bin/sh
  argv:
    - tenants
  timeout: 30s
output:
  parser: json
  max_stdout_bytes: 1048576
  max_stderr_bytes: 8192
examples:
  - title: Tenants
    args: {}
