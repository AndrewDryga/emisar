schema_version: 1
id: gcp.sql_server_ca_certs
title: gcloud sql ssl server-ca-certs list
kind: script
risk: low
description: List bounded Cloud SQL server CA identity, type, fingerprint, and expiry without certificate PEM.
side_effects: ["Read-only Cloud SQL API pagination.", "Uses a mode-0600 temporary response file and removes it before exit."]
args:
  - name: project
    type: string
    required: true
    description: Google Cloud project ID.
    validation: {pattern: "^[a-z][a-z0-9-]{4,28}[a-z0-9]$", max_length: 30}
  - name: instance
    type: string
    required: true
    description: Cloud SQL instance ID.
    validation: {pattern: "^[a-z](?:[a-z0-9-]{0,96}[a-z0-9])?$", max_length: 98}
  - name: limit
    type: integer
    required: false
    default: 100
    description: Maximum server CA certificates to return.
    validation: {min: 1, max: 500}
execution:
  script: {path: scripts/cloudsql.sh, interpreter: /bin/sh}
  argv: [server-ca-certs, "{{ args.project }}", "{{ args.instance }}", "{{ args.limit }}"]
  timeout: 60s
output:
  parser: json
  parser_required: true
  max_stdout_bytes: 1048576
  max_stderr_bytes: 16384
examples:
  - title: Application database server CAs
    args: {project: example-prod, instance: app-db}
