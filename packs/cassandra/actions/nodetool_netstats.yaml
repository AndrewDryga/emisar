schema_version: 1
id: cassandra.nodetool_netstats
title: nodetool netstats
kind: exec
risk: low
description: 'Show streaming + read repair stats: completed/pending bytes, files transferred, pool stats.'
side_effects: ["One JMX call.", "Read-only."]
args: []
execution:
  command: {binary: nodetool, argv: ["netstats"]}
  timeout: 30s
output:
  parser: text
  max_stdout_bytes: 65536
  max_stderr_bytes: 8192
examples:
  - title: Net stats
    args: {}
