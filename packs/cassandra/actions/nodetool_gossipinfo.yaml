schema_version: 1
id: cassandra.nodetool_gossipinfo
title: nodetool gossipinfo
kind: exec
risk: low
description: Per-peer gossip state — schema version, status, load, dc, rack, generation.
side_effects: ["One JMX call.", "Read-only."]
args: []
execution:
  command: {binary: nodetool, argv: ["gossipinfo"]}
  timeout: 30s
output:
  parser: text
  max_stdout_bytes: 131072
  max_stderr_bytes: 8192
examples:
  - title: Gossip view
    args: {}
