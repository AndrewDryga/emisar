schema_version: 1
id: rds.list_cluster_snapshots
title: aws rds describe-db-cluster-snapshots
kind: script
risk: low
description: List a bounded page of RDS cluster snapshots with source, status, engine, capacity, and encryption state.
side_effects: ["Read-only API pagination.", "Uses a mode-0600 temporary response file and removes it before exit."]
args:
  - name: limit
    type: integer
    required: false
    default: 100
    description: Maximum snapshots to return.
    validation: {min: 1, max: 1000}
  - name: next_token
    type: string
    required: false
    default: ""
    sensitive: true
    description: Opaque AWS CLI token from a previous result.
    validation: {pattern: "^[A-Za-z0-9+/=_-]*$", max_length: 4096}
execution:
  script: {path: scripts/rds_read.sh, interpreter: /bin/sh}
  argv: [cluster-snapshots, "{{ args.limit }}", "{{ args.next_token }}"]
  timeout: 60s
output:
  parser: json
  parser_required: true
  max_stdout_bytes: 4194304
  max_stderr_bytes: 8192
examples:
  - title: Cluster snapshots
    args: {}
