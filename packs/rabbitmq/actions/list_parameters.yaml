schema_version: 1
id: rmq.list_parameters
title: rabbitmqctl list_parameters
kind: exec
risk: low
description: List runtime parameters (federation upstreams, shovel definitions, etc).
side_effects:
  - One CLI call.
  - Read-only.
args: []
execution:
  command:
    binary: rabbitmqctl
    argv:
      - "list_parameters"
  timeout: 15s
output:
  parser: text
  max_stdout_bytes: 65536
  max_stderr_bytes: 8192
examples:
  - title: Parameters
    args: {}
