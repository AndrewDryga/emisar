services:
  memcached:
    image: memcached:${PACKTEST_VERSION:-1.6.45}
    command: ["-o", "track_sizes"]
    healthcheck:
      test: ["CMD", "pidof", "memcached"]
      interval: 5s
      retries: 10
