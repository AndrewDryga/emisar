schema_version: 1
id: frr.interfaces
title: vtysh -c "show interface brief"
kind: exec
risk: low
description: Interface brief — admin/protocol status, addresses, and VRF per interface as FRR sees them.
side_effects: ["One vtysh show command.", "Read-only."]
args: []
execution:
  command: {binary: vtysh, argv: ["-c", "show interface brief"]}
  timeout: 15s
output:
  parser: text
  max_stdout_bytes: 65536
  max_stderr_bytes: 8192
examples:
  - title: Interface status
    args: {}
