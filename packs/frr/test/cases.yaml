# Shutting a neighbour only means something against a real session, so this
# needs a second FRR speaker to peer with — a multi-container topology, not a
# privilege. Declared because that topology is not built.
# See .agent/kb/pack-behavior-environment-limits.md for what was measured.
risk_accountability:
  mode: complete
  exceptions:
    frr.bgp_neighbor_shutdown: requires_privileged_host
