schema_version: 1
id: caddy.version
title: caddy version
kind: exec
risk: low
description: Show Caddy binary version + build info.
side_effects: ["One forked process.", "Read-only."]
args: []
execution:
  command: {binary: caddy, argv: ["version"]}
  timeout: 15s
output:
  parser: text
  max_stdout_bytes: 4096
  max_stderr_bytes: 8192
examples:
  - title: Version
    args: {}
