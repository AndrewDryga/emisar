schema_version: 1
id: fs.head_file
title: head -n <N> <file>
kind: exec
risk: low
description: Show the first N lines of one file.
side_effects:
  - One read of the file.
  - Read-only.
args:
  - name: path
    type: string
    required: true
    description: File path.
    validation:
      pattern: "^/[a-zA-Z0-9_./\\-]{0,512}$"
      denied_paths:
        - /etc/shadow
        - /etc/gshadow
        - /etc/shadow-
        - /etc/gshadow-
        - /etc/ssl/private
        - /root/.ssh
        - /etc/ssh
        - /etc/sudoers
        - /etc/sudoers.d
        - /proc/kcore
  - name: lines
    type: integer
    required: false
    default: 100
    description: Lines.
    validation:
      min: 1
      max: 10000
execution:
  command:
    binary: head
    argv:
      - "-n"
      - "{{ args.lines }}"
      - "{{ args.path }}"
  timeout: 30s
output:
  parser: text
  max_stdout_bytes: 1048576
  max_stderr_bytes: 8192
  redact:
    - name: private-key-pem
      type: regex
      pattern: '(?s)-----BEGIN [A-Z0-9 ]*PRIVATE KEY-----.*?-----END [A-Z0-9 ]*PRIVATE KEY-----'
      replacement: '[REDACTED PRIVATE KEY]'
examples:
  - title: First 100 lines
    args:
      path: "/var/log/syslog"
