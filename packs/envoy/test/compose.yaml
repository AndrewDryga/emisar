services:
  envoy:
    image: envoyproxy/envoy:${PACKTEST_VERSION:-v1.39.0}${PACKTEST_DIGEST-}
    command: envoy -c /etc/envoy/envoy.yaml
    volumes:
      - ${EMISAR_ROOT}/packs/envoy/test/fixtures/envoy.yaml:/etc/envoy/envoy.yaml:ro
