schema_version: 1
id: nomad.leader
title: GET /v1/status/leader
kind: exec
risk: low
description: Show the current Raft leader address (host:port).
side_effects: ["One API call.", "Read-only."]
args: []
execution:
  command: {binary: nomad, argv: ["operator", "api", "/v1/status/leader"]}
  timeout: 15s
output:
  parser: text
  max_stdout_bytes: 1024
  max_stderr_bytes: 8192
examples:
  - title: Leader
    args: {}
