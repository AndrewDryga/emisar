schema_version: 1
id: consul.list_checks_critical
title: GET /v1/health/state/critical
kind: script
risk: low
description: List every check currently in CRITICAL state across the catalog.
side_effects: ["One API call.", "Read-only."]
search_terms:
  - what is failing
  - failing services
args: []
execution:
  script:
    path: scripts/api_request.sh
    interpreter: /bin/sh
  argv: [GET, /v1/health/state/critical]
  timeout: 30s
output:
  parser: json
  max_stdout_bytes: 1048576
  max_stderr_bytes: 8192
examples:
  - title: Critical checks
    args: {}
