# The tracer needs a real node with real traffic, which a SUT running a BEAM
# release and a load generator would be. Declared because that fixture is not
# built, not because a container cannot host it.
# See .agent/kb/pack-behavior-environment-limits.md for what was measured.
risk_accountability:
  mode: complete
  exceptions:
    beam.recon_trace_calls: requires_privileged_host
