# dep-age-gate allowlist — the escape hatch for an urgent dependency bump that
# must land BEFORE its release-age window elapses (see tools/cmd/depgate).
#
# emisar is a security product, so the normal rule is: a freshly-published
# dependency release waits out its cooldown window before it can merge. The one
# legitimate exception is an urgent security fix — a patch for an actively
# exploited CVE in a dependency — where waiting is the larger risk. That is a
# deliberate, reviewable decision, so it leaves a paper trail here rather than a
# silent flag or a CI override.
#
# One entry per line:  <ecosystem> <package> <version> <expires> <reason>
#   ecosystem  hex | go | npm
#   package    exact package/module name as it appears in the lockfile
#   version    exact version being allowed (hex/npm: "1.2.3", go: "v1.2.3")
#   expires    REQUIRED, YYYY-MM-DD — the last day this exemption applies
#   reason     REQUIRED, non-empty — the CVE / advisory id and why it can't wait
#
# Both are mandatory: a bare exemption is a hard error, and so is an entry whose
# date has passed — the gate fails until you remove it or set a new date with a
# fresh reason. An exemption also covers a DOWNGRADE, because a patched older
# line is a real answer to an urgent advisory; the date is what keeps that a
# decision rather than a permanent hole.
#
# Example (delete once real entries exist):
#   hex plug 1.99.1 2026-01-31 GHSA-xxxx-yyyy-zzzz — RCE in Plug.Conn, patched only in 1.99.1, actively exploited

hex oidcc 3.9.0 2026-09-16 EEF-CVE-2026-75759 (HIGH) — oidcc 3.8.0 accepts an encrypted ID token/JARM response without a nested signature (SSO auth-bypass class); fixed only in 3.9.0, on our SSO sign-in path. Can't wait out the 14-day minor-bump window; remove once 3.9.0 has aged past it.
