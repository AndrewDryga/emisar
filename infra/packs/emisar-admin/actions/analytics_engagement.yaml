schema_version: 1
id: emisar.admin.analytics.engagement
title: Product engagement and retention
kind: script
risk: low
description: Return the 50 most active accounts with run counts and last-use timestamps.
side_effects: [Read-only aggregate analytics.]
args:
  - name: days
    type: integer
    required: false
    default: 30
    description: Lookback window in days.
    validation: {min: 1, max: 3650}
execution:
  script: {path: scripts/callback.sh, interpreter: /bin/sh}
  argv:
    - "emisar.admin.analytics.engagement"
    - "days={{ args.days? }}"
  timeout: 30s
output: {parser: json, parser_required: true, max_stdout_bytes: 131072, max_stderr_bytes: 8192}
