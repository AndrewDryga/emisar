# Remote-run upload context for infra/. Keep Terraform source plus runtime
# payloads, packs, and the dependency lock; exclude local-only material.

# Repository and local tool state
.git
.agent/
.DS_Store

# Terraform state, plans, overrides, and provider downloads
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json
crash.log
crash.*.log

# Local variable files can contain secrets. Committed examples remain included.
*.tfvars
*.tfvars.json
!*.tfvars.example
!*.tfvars.json.example

# Developer documentation, checks, and root operational helpers are not run by HCP.
.tflint.hcl
AGENTS.md
README.md
/scripts/
tests/
