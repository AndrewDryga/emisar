# Build context is the REPO ROOT (see portal/Dockerfile). The image only
# needs source; everything below is either rebuilt inside the container
# (_build, deps via `mix deps.get`/`mix release`) or never COPY-ed at all.
.git
.github
.agent
**/.agent
.expert
**/.expert
.claude
.codex
bin
dev
**/_build
**/deps
**/.elixir_ls
**/node_modules
**/.env
**/.env.*
**/cover
**/erl_crash.dump
**/*.log
portal/apps/emisar_web/priv/cert
.DS_Store
**/.DS_Store
