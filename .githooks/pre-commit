#!/bin/sh
# Every committer enters the same staged-index check. Keep format semantics in
# tools/internal/devtool, where filenames and staged blobs are handled safely.
root=$(git rev-parse --show-toplevel) || exit 1
exec "$root/run" check staged
