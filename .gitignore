# Build artifacts
/bin/
/dist/
*.test
*.out
coverage.out

# Stray dev binaries — `go build` from inside a module dir writes here
# (named after the dir). Build the real ones via `make build`, which
# puts them under /bin/.
/runner/runner
/mcp/mcp
/emisar
/emisar-mcp

# Local runtime state from the example config
/runner/examples/var/

# Cross-compile output from `make build-all`
emisar-*-linux-*
emisar-*-darwin-*

# Test JSONL logs that might be dropped if a test forgets to t.TempDir()
events.jsonl
events.jsonl.cursor
events.jsonl.*

# Erlang/BEAM crash dumps — written to the CWD when the VM aborts.
erl_crash.dump

# Editor noise
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
.claude/scheduled_tasks.lock

# Local dev compose secrets — copy .env.example to .env and fill in.
/.env

docs/
