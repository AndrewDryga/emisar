schema_version: 1
id: es.cluster_health
title: GET /_cluster/health
kind: exec
risk: low
description: Cluster status (green/yellow/red), node count, shard counts. Read-only.
side_effects:
  - One GET request.
  - Read-only.
args: []
execution:
  command:
    binary: /bin/sh
    argv:
      - "-c"
      - "curl -sS -u \"$ELASTIC_USER:$ELASTIC_PASSWORD\" \"${ELASTIC_URL:-http://127.0.0.1:9200}/_cluster/health?pretty\""
  timeout: 15s
output:
  parser: json
  max_stdout_bytes: 8192
  max_stderr_bytes: 8192
examples:
  - title: Cluster health
    args: {}
