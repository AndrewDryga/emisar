schema_version: 1
id: mongo.replset_config
title: rs.conf()
kind: exec
risk: low
description: Replica set configuration — members, votes, priorities, settings.
side_effects:
  - One replSetGetConfig.
  - Read-only.
args: []
execution:
  command:
    binary: /bin/sh
    argv:
      - "-c"
      - "mongosh \"$MONGO_URI\" '--quiet' '--eval' 'JSON.stringify(rs.conf())'"
  timeout: 30s
output:
  parser: json
  max_stdout_bytes: 65536
  max_stderr_bytes: 8192
examples:
  - title: RS config
    args: {}
