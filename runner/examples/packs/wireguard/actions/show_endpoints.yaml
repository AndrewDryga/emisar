schema_version: 1
id: wg.show_endpoints
title: wg show all endpoints
kind: exec
risk: low
description: Per-peer remote endpoint (last known).
side_effects: ["One netlink query.", "Read-only."]
args: []
execution:
  command: {binary: wg, argv: ["show", "all", "endpoints"]}
  timeout: 15s
output:
  parser: text
  max_stdout_bytes: 32768
  max_stderr_bytes: 8192
examples:
  - title: Endpoints
    args: {}
