name: Release - MCP Bridge

run-name: Release ${{ github.ref_name }}

# The tag-selected workflow is deliberately only a trigger. The reusable
# workflow owns the release policy and every authority-bearing job. The full
# commit SHA is also the GCP `job_workflow_sha` trust anchor; rotate the workflow
# and federation condition together in a later reviewed change.

on:
  push:
    tags:
      - "mcp-v*.*.*"

concurrency:
  group: mcp-release-${{ github.ref }}
  cancel-in-progress: false

permissions: {}

jobs:
  release:
    name: Trusted MCP bridge release
    permissions:
      attestations: write
      contents: write
      id-token: write
    uses: AndrewDryga/emisar/.github/workflows/mcp-release-trusted.yml@ff1dd7f5902589a61a308f1805c905b4a8f6ac8f
